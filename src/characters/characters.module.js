// Phase 4 — Characters NestJS module (placeholder)
// Will contain: module, controller, service
// Endpoints: GET /characters, GET /characters/:id, GET /characters/:id/teams, GET /characters/:id/events
